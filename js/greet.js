let name = document.getElementById("usersName");
//return an Element object that represent HTML element with specific id

       
function getName() {
    let userName = usersName.value; 
    //get what the user placed into the text field
    console.log("Hello" + " " + usersName.value);
    //Place Hello using a string + what the user placed into the text field 
    //"" empty string to create a space

            
        
        }
